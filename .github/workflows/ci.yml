name: CI — Build, Test & Docker

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore

    - name: Test
      run: dotnet test --no-build --verbosity normal

    - name: Docker build
      run: docker build -t thesnaxers:latest -f The-Snaxers/Dockerfile .

 #   # Om du vill pusha till Azure Container Registry, avkommentera nedan och se till att du har satt upp dina secrets i GitHub
 #   - name: Log in to Azure Container Registry
 #     uses: azure/docker-login@v2
 #     with:
 #       login-server: ${{ secrets.ACR_LOGIN_SERVER }}
 #       username: ${{ secrets.ACR_USERNAME }}
 #       password: ${{ secrets.ACR_PASSWORD }}

 #   - name: Build and Push Docker image
 #     run: |
 #       # Vi taggar bilden med både 'latest' och ett unikt ID (SHA)
 #       docker build -t ${{ secrets.ACR_LOGIN_SERVER }}/thesnaxers:latest -t ${{ secrets.ACR_LOGIN_SERVER }}/thesnaxers:${{ github.sha }} -f Dockerfile .
 #       docker push ${{ secrets.ACR_LOGIN_SERVER }}/thesnaxers --all-tags